name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.11
        with:
          heroku_api_key: ${{b3536312-ad73-48e5-a2d8-b73506504b03}}
          heroku_app_name: "github-cd1"
          heroku_email: "stevaniek@yahoo.com"